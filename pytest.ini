[pytest]
pythonpath = src
testpaths = tests
markers =
    integration: exercises local FFmpeg or infrastructure boundaries
    redis_integration: tests that require a disposable Redis database
    live_validation: opt-in end-to-end validation with live HLS and Redis
